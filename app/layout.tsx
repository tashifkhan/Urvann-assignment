import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Urvann Mini Store - Beautiful Plants for Your Home',
  description: 'Discover beautiful, healthy plants that bring life to your space. Browse our curated collection of indoor, outdoor, and specialty plants.',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}